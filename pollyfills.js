// pollyfills are a piece of a js code which is used to provide modern functionalities to the old browsers which do not actually support it

//pollyfill for map
//myMap is the name of our own map function
function callback(number)
{
    return number**2;
}

Array.prototype.myMap=function(callback){
    let temparr=[];
    for(let i=0)
};